StatChasers is:

White-first

Clean

Navy structure

Gold accent

Strong hierarchy

So let’s fix the scheme at the system level instead of tweaking pieces.

🔥 The Core Problem

You currently have:

Dark hero
Dark search bar
Dark filters
Dark counters
Light page below

That split makes the page feel disjointed.

You either:

Go fully dark (not your brand)

Or go fully white with navy structure (your brand)

You should go option 2.

✅ Recommended Command Index Color Reset
1️⃣ Kill the dark hero gradient.

Replace with white.

Yes — white.

StatChasers should feel confident and clean.

2️⃣ New Header Structure (StatChasers-native)

Background: #ffffff

Main title: Navy #0f172a

Gold underline stays.

Subtext: muted navy #475569

Search bar:

White

1px soft border

Subtle shadow

Fantasy Starters count:

White card

Navy text

Gold accent line at top

🎨 Proposed Final Color System

Use this across the whole tool:

:root {
  --sc-bg: #ffffff;
  --sc-navy: #0f172a;
  --sc-muted: #64748b;
  --sc-border: rgba(15,23,42,0.12);
  --sc-soft: rgba(15,23,42,0.05);

  --sc-gold-1: #caa14a;
  --sc-gold-2: #f4d27a;
}

No more dark background sections.

✨ How It Should Feel

Instead of:
Dark. Heavy. Developer dashboard.

It should feel like:
Financial SaaS platform.
Premium analytics.
FantasyPros-level polish.

White = authority.
Dark gradient = gaming UI.

You’re not building Madden.
You’re building a fantasy analytics brand.

🔥 Specific Fixes to That Screenshot
A) Remove dark background behind search and filters

Make entire top section white.

Add a subtle bottom divider:

.sc-header {
  background:#fff;
  border-bottom: 1px solid var(--sc-border);
  padding: 40px 0 30px;
}
B) Search Bar Upgrade

Current: dark input

New:

.sc-search {
  background:#fff;
  border:1px solid var(--sc-border);
  border-radius:14px;
  box-shadow: 0 6px 18px rgba(15,23,42,.05);
}
C) Position Pills

Make them light with colored outline, not filled blobs.

Example:

.sc-filter-pill {
  background:#fff;
  border:1px solid var(--sc-border);
  border-radius:999px;
  padding:6px 14px;
  font-weight:600;
  color: var(--sc-navy);
}

.sc-filter-pill.active {
  border-color: var(--sc-gold-1);
  box-shadow: 0 0 0 2px rgba(202,161,74,.15);
}

Much cleaner.

D) “352 Fantasy Starters” Card

Make it a white stat card:

.sc-stat-card {
  background:#fff;
  border:1px solid var(--sc-border);
  border-radius:16px;
  padding:16px 20px;
  box-shadow: 0 8px 24px rgba(15,23,42,.06);
}

Add thin gold top border:

.sc-stat-card::before{
  content:"";
  display:block;
  height:3px;
  background:linear-gradient(90deg, var(--sc-gold-1), var(--sc-gold-2));
  border-radius:999px 999px 0 0;
}

Now it looks premium.